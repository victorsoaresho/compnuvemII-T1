DB_HOST=""
DB_PORT=""
DB_NAME=""
DB_USER=""
DB_PASSWORD=""

# Padrão do docker-compose em database/: jdbc:postgresql://localhost:5432/compnuvem-t1
DATABASE_URL=""

# Google Cloud Pub/Sub: project_id, subscription e topic ficam em backend-v1/credentials/credentials.json (bloco pubsub).
# Subir o app com diretório de trabalho em backend-v1 (Run do Spring Boot).